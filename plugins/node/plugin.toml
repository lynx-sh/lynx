[plugin]
name        = "node"
version     = "0.1.0"
description = "Node.js version detection — reads .node-version/.nvmrc; shows node_version prompt segment"
authors     = ["proxikal"]

[load]
lazy  = false
hooks = []

[deps]
binaries = []
plugins  = []

[exports]
functions = ["node_gather_state"]
aliases   = []

[contexts]
disabled_in = ["agent", "minimal"]
