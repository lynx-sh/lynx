# syntax-highlight plugin — init.zsh
# Vendors zsh-syntax-highlighting with theme-integrated colors from Lynx.
# ZSH_HIGHLIGHT_STYLES are pre-set by lx init from the active theme.

source "${0:A:h}/../vendor/zsh-syntax-highlighting.zsh"
