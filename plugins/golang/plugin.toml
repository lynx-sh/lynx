[plugin]
name        = "golang"
version     = "0.1.0"
description = "Go version detection — reads go.mod for the required Go toolchain version; shows golang_version prompt segment"
authors     = ["proxikal"]

[load]
lazy  = false
hooks = []

[deps]
binaries = []
plugins  = []

[exports]
functions = ["golang_gather_state"]
aliases   = []

[contexts]
disabled_in = ["agent", "minimal"]
