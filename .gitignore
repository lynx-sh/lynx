/target
**/*.rs.bk
.DS_Store
*.swp

# Secrets and credentials — never commit
.env
.env.*
*.pem
*.key
*.p12
*.pfx
credentials
credentials.*
*.secret
# Claude Code local state (skills are tracked, memory is not)
.claude/memory/
