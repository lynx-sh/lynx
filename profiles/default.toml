# Lynx default profile
# This is the base profile. Other profiles can extend it with:
#   extends = "default"

name    = "default"
theme   = "default"
plugins = ["git", "fzf"]

# context_override = "interactive"   # suggested context — not enforced

[env]
# EDITOR = "nvim"
# PAGER  = "less"

[aliases]
# Aliases are only loaded in interactive context (context filter applies).
# ll = "ls -la"
